# Copy this file to `infra/postgres/.env` when you want to override the
# local Postgres container defaults used by `make postgres-up`.

POSTGRES_DB=livepair
POSTGRES_USER=livepair
POSTGRES_PASSWORD=livepair
POSTGRES_PORT=5432
